# AI ERP Assistant — Backend Dependencies
# ========================================
# These are installed into a package directory and zipped for Lambda deployment.

fastapi==0.104.1
mangum==0.17.0
boto3>=1.34.69
python-multipart==0.0.6
uvicorn==0.24.0
